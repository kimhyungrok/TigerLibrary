<resources xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <!-- custom progress dialog -->
    <style name="CustomProgressDialog" parent="@android:style/Theme.Dialog">
        <item name="android:windowFrame">@null</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowTitleStyle">@null</item>
        <item name="android:windowAnimationStyle">@android:style/Animation.Dialog</item>
        <item name="android:windowSoftInputMode">stateUnspecified|adjustPan</item>
        <item name="android:backgroundDimEnabled">true</item>
        <item name="android:background">@android:color/transparent</item>
    </style>

    <!--popup window-->
    <style name="popup_window_animation_above">
        <item name="android:windowEnterAnimation">@anim/quick_action_popup_window_show_above</item>
        <item name="android:windowExitAnimation">@anim/quick_action_popup_window_hide_above</item>
    </style>

    <style name="popup_window_animation_below">
        <item name="android:windowEnterAnimation">@anim/quick_action_popup_window_show_below</item>
        <item name="android:windowExitAnimation">@anim/quick_action_popup_window_hide_below</item>
    </style>
</resources>
